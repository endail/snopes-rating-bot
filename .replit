#run = "while true; do python -m pip install --upgrade pip && poetry install && python main.py; done"

run = "pip install --upgrade pip && poetry install && git commit -am 'install packages' || python main.py"

#onboot = "python main.py"
onboot = "pip install --upgrade pip && poetry install && git commit -am 'install packages'"

# entrypoint = "main.py"

language = "python3"

[packager]
language = "python-python3-poetry"

audio = false

